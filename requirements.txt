fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
python-dotenv>=1.0
supabase>=2.5
pytest>=8.2
pytest-cov>=5.0
httpx>=0.27
anthropic>=0.40
langgraph>=0.4
langgraph-checkpoint-postgres>=2.0
psycopg[binary,pool]>=3.2
langsmith>=0.1
Pillow>=10.4  # test fixtures only: synthesizes invoice images for OCR tests
PyYAML>=6.0  # SPEC-F4-01: external selector map (src/rpa/selectors/aeat_m303.yml)
playwright>=1.61  # SPEC-F4-02/04/05: AEAT RPA automation
arq>=0.28  # SPEC-F4-worker: ARQ + Redis job queue
pypdf>=6.14  # SPEC-F4-05: justificante PDF text-layer verification
python-multipart>=0.0.32  # SPEC-F5-01: multipart/form-data for /facturas/ocr
redis>=5.0  # SPEC-F5-09 correction: /health pings Redis directly (arq's own dependency, now a direct import too)
